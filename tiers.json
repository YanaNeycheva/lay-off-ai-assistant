{
  "//": "Tier -> model map per tool (Seam 5). The generator (tools/gen_agents.py) emits each agent's `model:` from here based on its tier, so model IDs live in ONE place, never baked into agent files. To change models: pick from the tool's available list and update the 3 lines, then rerun `python tools/gen_agents.py`. See PORTING-PLAN.layoff.md §6.1.",
  "opencode": {
    "//": "OpenCode Zen FREE models (no provider auth needed) — read live from the app's model selector on 2026-09-15; IDs per opencode.ai/docs/zen. `mid` = big-pickle, proven end-to-end in the live benefits-slice run. When you auth a real provider (OpenAI/Anthropic/Google/Copilot) in OpenCode, replace these with its provider/model-id and regenerate.",
    "strong": "opencode/nemotron-3-ultra-free",
    "mid": "opencode/big-pickle",
    "cheap": "opencode/nemotron-3.5-lightning-free"
  }
}
